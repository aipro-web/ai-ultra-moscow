# AI Ultra / Claude instructions

The canonical project workflow is defined in:
- `PROJECT_LOCK.md`
- `workspace/_LOCKS.json`

Always read those files first. Use patch-only work by default. Never rewrite or re-audit LOCKED/APPROVED scopes unless the user explicitly requests UNLOCK or FULL CHECK.
